// @cc-interact/react — opt-in browser client for cc-interact.
//
// Phase 3 fills this with the realtime stream layer (createEventStream, EventStreamProvider,
// useEventStream), the query primitives (createApiClient, request, createOptimisticMutation,
// scopedKey), and the app shell (AppShell, NotificationsBar, ConnectionFrame, SubjectProvider).
export const VERSION = '0.0.0';
