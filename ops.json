{"v":1,"lamport":6,"ops":[{"kind":"set_title","title":"Yasyf runs several concurrent Claude sessions per repo; treat shared working copies as live and origins as moving targets"},{"kind":"set_body","body":"Yasyf's repos (cc-present, cc-context, captain-hook, cc-skills) routinely have another live Claude session editing the shared working copy and pushing to origin mid-task — releases can be cut underneath you (captain-hook went 8.0.0, then 8.1.0, then 8.2.0 by a parallel session during one 2026-07-03 work session, so the planned tag became 8.3.0).\n\n**Why:** Two agents assuming they own the checkout clobber each other; a rebase target can move between fetch and push.\n\n**How to apply:** Never park or commit another session's uncommitted edits. Do rebases, conflict resolution, and test runs in an isolated `jj workspace add` (jj-colocated repos) or `git worktree`; pure history operations (`jj rebase -s`, `jj bookmark set`) are safe and carry the live session's `@` along as a descendant. Fetch immediately before any push and re-rebase rather than force. Verify a \"release already exists\" surprise (`git ls-remote --tags`, registry JSON) before picking the next version. The cc-notes task store also gets reconciled/pruned by hooks in these repos — re-check a filed task still exists before relying on it. Related: [[publish-via-ci-only]].\n\n**Committing in a shared checkout is unsafe even with pathspec discipline.** On 2026-07-09/10 (cc-interact + cc-present), `git add \u003cmy-files\u003e \u0026\u0026 git commit` and even `git commit --only -- web/src` produced commits carrying a sibling session's concurrently staged work (transient file deletions, a whole staged Go/CLI feature set landed under my commit message) — the index and HEAD are shared mutable state, and the sibling can stage or amend between any two of your git commands. When a sibling session is live, make commits from a private `git worktree` / `jj workspace` pointed at the same ref, or hand the final commit to a moment when the tree is quiet — never interleave commit surgery (reset/amend/split) with a live sibling; two rounds of it compounded the tangle.\n\nVersion numbers are moving targets too: run the triple-check (origin/main position + `git tag -l 'vX.Y*'` after `fetch --tags` + registry JSON) at THREE moments — before starting, before pushing, and again immediately before tagging. On 2026-07-07 captain-hook's planned 8.6.0 was taken days before, and the re-targeted 8.7.0 was tagged AND on PyPI by a parallel session within the same working session (caught only by the pre-tag re-check; shipped as 8.8.0 after a changelog-only rebase). Assume the version you planned is stale until the pre-tag check passes."}]}